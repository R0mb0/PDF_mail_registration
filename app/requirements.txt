# GUI framework
PySide6>=6.6

# Data handling
pandas>=2.0
openpyxl>=3.1        # .xlsx export
# SQL filter mode and SQL export both use Python's built-in sqlite3 module --
# no duckdb/SQLAlchemy dependency, one less thing that can fail to install.

# PDF handling
pypdf>=4.0           # AcroForm field extraction (matches LaTeX_Template field names)
# PDF page rendering for the preview panes uses PySide6's own QtPdf /
# QtPdfWidgets modules (QPdfDocument, QPdfView) -- bundled with PySide6
# itself, no separate package needed.
