# GUI framework
PySide6>=6.6

# Data handling
pandas>=2.0
duckdb>=0.10        # SQL filter engine (queries pandas DataFrames directly)
openpyxl>=3.1        # .xlsx export

# PDF handling
pypdf>=4.0           # AcroForm field extraction (matches LaTeX_Template field names)
PyMuPDF>=1.24         # PDF page rendering for the preview panes
